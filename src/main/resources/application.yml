spring:
  datasource:
    url: jdbc:mysql://localhost:3306/employee_management_system?serverTimezone=UTC
    username: root
    password: Tunisie2mpi.
    # driver-class-name: com.mysql.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL5Dialect

  profiles:
      active: dev
server:
    port: 8081